import { UserProfile } from "../interface";

const Profile = ({ user }: { user: UserProfile }) => {
  return <div className="w-2/5 md:w-1/3 border-2 border-gray-900">Profile</div>;
};

export default Profile;
